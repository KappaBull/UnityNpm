image: kappabull/unitynpmci:latest
update_repository:
  only:
    - master
  script:
    - echo $SSHKEY > /root/.ssh/github_id_rsa
    - chmod 600 /root/.ssh/github_id_rsa
    - /usr/local/bin/uniNpmCI