image: kappabull/unitynpmci:latest
update_repository:
  only:
    - master
  script:
    - echo "Host github.com¥nStrictHostKeyChecking no¥nUserKnownHostsFile /dev/null¥nIdentityFile ~/.ssh/github_id_rsa" > ~/.ssh/config
    - echo $SSHKEY > ~/.ssh/github_id_rsa